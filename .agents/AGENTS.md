# Project Rules for Hackathon / PulseShift

## Localhost Server Verification
- Whenever major updates, features, or UI changes are implemented, run and test the application on `http://localhost:8000`.
- Ensure the server is actively hosted on `localhost:8000` so the user can immediately preview and interact with the application.
